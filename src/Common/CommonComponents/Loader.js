import React from 'react'
import { Spinner } from 'react-bootstrap'

const Loader = () => {
    return (
        <div>
            {/* loader for all tables */}
            <Spinner animation="border" />
        </div>
    )
}

export default Loader
